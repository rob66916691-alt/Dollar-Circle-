import { Injectable } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Contribution } from './contribution.entity'; import { CreateContributionDto } from './dto/create-contribution.dto'; @Injectable() export class ContributionsService { constructor(@InjectRepository(Contribution) private repo:Repository<Contribution>){} create(contributorId:string,d:CreateContributionDto){return this.repo.save(this.repo.create({contributorId,requestId:d.requestId,amount:d.amount.toFixed(2)}))} mine(contributorId:string){return this.repo.find({where:{contributorId},order:{createdAt:'DESC'}})} }
