import { Injectable } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { AssistanceRequest,RequestStatus } from './assistance-request.entity'; import { CreateRequestDto } from './dto/create-request.dto';
@Injectable() export class RequestsService { constructor(@InjectRepository(AssistanceRequest) private repo:Repository<AssistanceRequest>){} create(userId:string,d:CreateRequestDto){return this.repo.save(this.repo.create({userId,title:d.title,description:d.description,amountRequested:d.amountRequested.toFixed(2)}))} mine(userId:string){return this.repo.find({where:{userId},order:{createdAt:'DESC'}})} approved(){return this.repo.find({where:{status:RequestStatus.APPROVED},order:{createdAt:'DESC'}})} }
